#include <iostream>

using namespace std;

int arr[20000][20000];
int V, E, K;
int u, v, w;

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(NULL); cout.tie(NULL);

    cin >> V >> E;

    cin >> K;

    for(int i = 0; i < E; i++) {
        cin >> u >> v >> w;
        
    }


    return 0;
}